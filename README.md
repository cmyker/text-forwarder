# text-forwarder
Lightweight Android utility that receives shared text from any app and forwards it to a user-selected target app. Remembers the chosen destination and works system-wide via the standard Android Share menu.
